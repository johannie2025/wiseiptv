@RunWith(AndroidJUnit4.class)public class ActivationActivityTest {    @Rule    public ActivityScenarioRule<ActivationActivity> rule =        new ActivityScenarioRule<>(ActivationActivity.class);    @Test    public void btnAccess_doesNotCrash() {        // Attendre que l'UI soit chargée        onView(withId(R.id.btn_access))            .check(matches(isDisplayed()));        // Simuler le clic        onView(withId(R.id.btn_access))            .perform(click());        // Vérifier que MainActivity est lancée        intended(hasComponent(MainActivity.class.getName()));    }}